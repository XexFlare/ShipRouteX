node_modules/
apps/*/dist/
packages/*/dist/
apps/*/build/
packages/*/build/
apps/*/coverage/
packages/*/coverage/

.env
.env.*
!.env.example

*.log
npm-debug.log*

.DS_Store
Thumbs.db

.vscode/*
!.vscode/extensions.json
.idea/

*.tsbuildinfo

# Netlify CLI local state (created by `netlify link`/`netlify dev`)
.netlify/

# Generated by apps/api/scripts/copy-netlify-assets.mjs (part of the
# apps/api Netlify build command) — staged data files bundled into the
# Netlify Function, not source.
apps/api/netlify/functions/_bundled/

# Reference material used to research the SeaRoute project this codebase is
# inspired by (see docs/searoute-architecture-analysis.md) — third-party
# source under its own license, not part of ShipRoutesX, and too large to
# ship in this repo. Safe to delete locally; nothing here depends on it.
searoute-master.zip
extracted/
